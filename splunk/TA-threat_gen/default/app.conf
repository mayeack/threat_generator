[id]
name = TA-threat_gen
version = 1.3.3

[install]
state = enabled
is_configured = false
build = 10

[ui]
is_visible = false
label = ThreatGen Add-on

[launcher]
author = ThreatGen
description = Parsing, indexed-field extraction, and HEC source routing for ThreatGen APT simulation logs (Cisco ASA with VPN sessions, Sysmon, Linux secure, Stream DNS/HTTP, WinEventLog). 1.3.3 indexes Windows Security as sourcetype WinEventLog:Security so ES predefined Kerberos discovery (Validate) matches; 1.3.2 linux_secure / Sysmon EA fixes retained.
version = 1.3.3

[package]
id = TA-threat_gen
check_for_updates = false
